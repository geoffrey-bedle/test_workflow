name: dev deployment
on:
  push:
    branches:
      - dev

jobs:
  deploy-dev:
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up SSH
        run: echo SSH connection from ${{ vars.ENV_NAME }} branch !
      
  
