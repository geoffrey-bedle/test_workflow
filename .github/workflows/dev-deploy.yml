name: dev deployment
on:
  push:
    branches:
      - dev

jobs:
  deploy-dev:
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: List Environment Variables
        run: env

      - name: Set up SSH
        run: echo SSH connection from ${{ env.ENV_NAME }} branch !
      
  
